import Alpine from 'alpinejs'
import component from 'alpinejs-component'

import './main.css'

window.Alpine = Alpine
Alpine.plugin(component)
Alpine.start()// import { setupCounter } from './counter.ts'